odoo.define("base.classic", function (require) {
  const { asds } = require("asd"),
    a = require("a");
  const asd = "asd";

  return {
    /** This is a parameter */
    foo: 123,
    a,
    asds,
  };
});
